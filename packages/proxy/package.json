{
  "name": "@serenity/proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit && wrangler deploy --dry-run --outdir build",
    "dev": "wrangler dev --local --port 8787",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@serenity/classifier": "0.1.0",
    "@serenity/core": "0.1.0",
    "@upstash/ratelimit": "^2.0.8",
    "@upstash/redis": "^1.38.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260908.1",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "4.129.1"
  }
}
